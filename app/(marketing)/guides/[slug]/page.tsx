import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { GUIDES } from "@/content/guides";
import { articleLd, breadcrumbLd, JsonLd } from "@/lib/seo/jsonld";
export const dynamicParams=true;export function generateStaticParams(){return GUIDES.map((g)=>({slug:g.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const g=GUIDES.find((x)=>x.slug===slug);return g?{title:g.title,description:g.description,alternates:{canonical:`/guides/${slug}`},openGraph:{type:"article"}}:{};}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=GUIDES.find((x)=>x.slug===slug);if(!g)notFound();return <><JsonLd data={[breadcrumbLd([{name:"Home",path:"/"},{name:"Guides",path:"/guides"},{name:g.title,path:`/guides/${slug}`}]),articleLd({headline:g.title,description:g.description,path:`/guides/${slug}`,datePublished:"2026-07-20"})]}/><article className="mx-auto max-w-3xl px-4 py-12 sm:px-6"><nav className="text-xs text-muted-foreground"><Link href="/guides">Guides</Link> / {g.title}</nav><header className="border-b pb-8"><p className="mt-6 text-sm font-bold uppercase tracking-widest text-signal">{g.readMinutes} minute guide</p><h1 className="mt-2 text-4xl font-bold tracking-tight sm:text-5xl">{g.title}</h1><p className="mt-4 text-lg text-muted-foreground">{g.description}</p></header><div className="space-y-10 py-10">{g.sections.map((s)=><section key={s.heading}><h2 className="text-2xl font-bold">{s.heading}</h2>{s.paragraphs.map((p)=><p key={p.slice(0,24)} className="mt-4 leading-7 text-muted-foreground">{p}</p>)}{s.bullets&&<ul className="mt-4 list-disc space-y-2 pl-6 text-muted-foreground">{s.bullets.map((b)=><li key={b}>{b}</li>)}</ul>}</section>)}</div><div className="rounded-2xl bg-primary p-6 text-primary-foreground"><h2 className="text-xl font-bold">Check your document automatically</h2><p className="mt-2 text-sm opacity-75">Extract the fields, then run deterministic shipping checks before export.</p><Link href="/app/scan" className="mt-4 inline-flex min-h-11 items-center rounded-lg bg-signal px-4 font-bold text-signal-foreground">Parse a document free</Link></div></article></>;}
